import React from "react";
import PropTypes from "prop-types";
import { css } from "@emotion/react";

const Logo = ({ color }) => <div>Logo</div>;

Logo.propTypes = {
  color: PropTypes.string,
};

export default Logo;
